# Core dependencies
faster-whisper>=0.9.0
numpy>=1.20.0
resampy>=0.4.0
soundfile>=0.12.1

# Frame device communication
# Note: frame_msg and frame_ble might need to be installed from the Frame SDK
# If these are custom packages, you may need to install them separately

# Audio processing
PyAudio>=0.2.13
ffmpeg-python>=0.2.0

# Machine learning backend
torch>=2.0.0
tqdm>=4.65.0

# Optional: for development
pytest>=7.3.1
black>=23.3.0
flake8>=6.0.0